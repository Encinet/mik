name: mik
version: '1.0'
main: org.encinet.mik.Mik
loader: org.encinet.mik.MikLoader
api-version: '1.21'
authors: [ Noctiro ]
dependencies:
  bootstrap:
    packetevents:
      load: BEFORE
      required: true
      join-classpath: true
    LuckPerms:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: false
  server:
    packetevents:
      load: BEFORE
      required: true
      join-classpath: true
    LuckPerms:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: false
